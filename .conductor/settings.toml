"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm install"
run = "npx concurrently -n server,web -c blue,green \"npm:dev:server\" \"npx vite --port $CONDUCTOR_PORT\""
run_mode = "concurrent"
